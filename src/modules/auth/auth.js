/*

TODO: port to react-router

import angular from 'angular'

export default 'cyb.auth'

let m = angular.module('cyb.auth')

m.factory("AuthRequireResolver", function ($q, AuthService) {
  return $q(function (resolve, reject) {
    if (AuthService.requireUser()) {
      resolve()
    } else {
      reject()
    }
  })
})

*/